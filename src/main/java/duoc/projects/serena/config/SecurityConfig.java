package duoc.projects.serena.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {

    // Se ha eliminado la definición de passwordEncoder para evitar duplicidad.
    // El passwordEncoder se define en SpringSecurityConfig.java

}
